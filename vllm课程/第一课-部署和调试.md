```
ss -ltnp | grep 13311
```

```
timeout 2 bash -c "cat < /dev/null > /dev/tcp/127.0.0.1/13311" && echo "tcp ok" || echo "tcp fail"
```

```
curl -s --noproxy '*' http://127.0.0.1:13311/v1/models | jq .
```

```
curl -s --noproxy '*' http://127.0.0.1:30000/v1/chat/completions   -H "Content-Type: application/json"   -d '{
    "model": "DeepSeek-R1-Distill-Qwen-1.5B",
    "messages": [{"role": "user", "content": "用20字介绍vLLM"}],
    "max_tokens": 30,
    "temperature": 0.6
  }' | jq -r '.choices[0].message.content'
```

vscode调试

attach没成反正

直接debug启动

jsonl文件

```
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Current File",
            "type": "debugpy",
            "request": "launch",
            // "program": "${file}",
            "console": "integratedTerminal",
            "module": "sglang.launch_server",
            "args": [
                "--model-path",
                "${env:HOME}/model_download/DeepSeek-R1-Distill-Qwen-1.5B",
                "--host",
                "0.0.0.0",
                "--port",
                "30000"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}/python",
                "PYDEVD_DISABLE_FILE_VALIDATION": "1"
            },
            "justMyCode": false,
            "subProcess": true
        },
        {
            "name": "Attach to debugpy (port 5678)",
            "type": "debugpy",
            "request": "attach",
            "connect": {
                "host": "localhost",
                "port": 5678
            },
            "pathMappings": [
                {
                "localRoot": "${workspaceFolder}",
                "remoteRoot": "${workspaceFolder}"
                }
            ]
        }
    ]
}
```

